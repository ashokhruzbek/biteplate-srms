// Observer - barcha notifier class-lar uchun contract

class Observer {
  update(order) {
    throw new Error("update() method observer class-da implement qilinishi kerak");
  }
}

module.exports = Observer;
